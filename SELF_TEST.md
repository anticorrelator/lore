# Memory System Self-Test

Run `/self-test` to evaluate this system.

The full test protocol lives in `skills/self-test/SKILL.md`. It evaluates 8 dimensions of memory system health, produces scored results, and compares against previous runs.
