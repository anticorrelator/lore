# Knowledge Store Protocol

You have access to a per-project knowledge store at `~/.project-knowledge/repos/`. Knowledge is loaded automatically at session start via hooks. Use `/memory` for explicit operations.
